# minesweeper-raylib-gui
A desktop Minesweeper game featuring an interactive GUI built in C++ using the Raylib framework. Includes user authentication via login validation, a custom grid layout ($9 \times 9$) with dynamic neighbor-mine tracking, persistent save/load features using file handling streams, and a retro-inspired game loop interface.
